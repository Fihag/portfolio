{
  "name": "llmlife",
  "version": "0.1.0",
  "description": "LLMLife · LLM 人生模拟器 - 行动点日程制人生 + 抽卡伙伴 (TokenGacha 精神续作)",
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "serve": "npx serve . -l 8124",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write \"**/*.{js,json,css,html,md}\"",
    "format:check": "prettier --check \"**/*.{js,json,css,html,md}\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "roster": "node tools/build-roster.mjs",
    "sync:sw": "node tools/sync-sw.mjs",
    "build:standalone": "node tools/build-standalone.mjs"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jsdom": "^24.1.0",
    "prettier": "^3.3.3",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
